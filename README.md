# SinkShift

A ship sinking survival game, played in the browser.

The game is **SinkShift**. The repo and folder are named `Jacks-Sinking-Ship`
for historical reasons — use SinkShift anywhere a person will read it.

Open `index.html` in a browser to play. No build step, no dependencies.
